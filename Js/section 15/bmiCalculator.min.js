function bmiCalculator(o,r){let u=Math.round(o/Math.pow(r,2));return u<18.5?"Your BMI is "+u+", so you are underweight.":u>18.5&&u<24.9?"Your BMI is "+u+", so you have a normal weight.":u>24.9?"Your BMI is "+u+", so you are overweight.":void 0}bmiCalculator(82,1.8);